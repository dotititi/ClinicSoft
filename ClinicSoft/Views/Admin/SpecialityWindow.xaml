<Window x:Class="ClinicSoft.Views.Admin.SpecialityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Специальности" Height="400" Width="400"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="TxtNewSpeciality" Height="36" Margin="0,0,0,8"/>
        <Button Content="Добавить" Height="36" Click="BtnAdd_Click" Grid.Row="0" HorizontalAlignment="Right" Width="100"/>

        <DataGrid x:Name="SpecialityGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Content="Закрыть" Grid.Row="2" Height="36" Click="BtnClose_Click" HorizontalAlignment="Right" Width="100"/>
    </Grid>
</Window>