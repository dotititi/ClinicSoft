<Page x:Class="ClinicSoft.Views.Registrator.RegistratorLabTestsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Анализы пациента">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,16">
            <Button x:Name="BtnBack" 
                    Content="Назад" 
                    Padding="12,8"
                    Background="Transparent"
                    BorderThickness="0"
                    Foreground="#1976D2"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Cursor="Hand"
                    Click="BtnBack_Click"
                    Margin="0,0,16,0"/>
            <TextBlock x:Name="PatientHeader" 
                       FontSize="20" FontWeight="Bold"
                       Foreground="#1976D2"
                       VerticalAlignment="Center"/>
        </StackPanel>
        <Border Grid.Row="1"
                Background="White"
                BorderBrush="#BDBDBD"
                BorderThickness="1"
                CornerRadius="4"
                Height="36"
                Margin="0,0,0,16">
            <Grid>
                <TextBox x:Name="SearchBox"
                         Background="Transparent"
                         BorderThickness="0"
                         VerticalContentAlignment="Center"
                         Padding="8,0,8,0"
                         TextChanged="SearchBox_TextChanged"
                         Foreground="#212121"/>
                <TextBlock x:Name="SearchPlaceholder"
                           Text="Поиск по врачу или названию анализа..."
                           Foreground="#9E9E9E"
                           VerticalAlignment="Center"
                           Margin="8,0,8,0"
                           IsHitTestVisible="False"/>
            </Grid>
        </Border>
        <TabControl Grid.Row="2">
            <TabItem Header="Назначенные">
                <DataGrid x:Name="PendingOrdersGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          RowHeight="60">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Height" Value="36"/>
                            <Setter Property="Padding" Value="8,4"/>
                            <Setter Property="Background" Value="#1976D2"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Дата" Binding="{Binding OrderedAt, StringFormat='dd.MM.yyyy'}" Width="110" MinWidth="70"/>
                        <DataGridTextColumn Header="Врач" Binding="{Binding DoctorName}" Width="220" MinWidth="190"/>
                        <DataGridTemplateColumn Header="Анализы" Width="*" MinWidth="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding TestTypes}"
                                               TextWrapping="Wrap"
                                               TextTrimming="CharacterEllipsis"
                                               Padding="8,4"
                                               VerticalAlignment="Center"
                                               ToolTip="{Binding TestTypes}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="130" MinWidth="60"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Завершённые">
                <DataGrid x:Name="CompletedOrdersGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          RowHeight="60"
                          CanUserAddRows="False">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Height" Value="36"/>
                            <Setter Property="Padding" Value="8,4"/>
                            <Setter Property="Background" Value="#4CAF50"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Дата" Binding="{Binding OrderedAt, StringFormat='dd.MM.yyyy'}" Width="110" MinWidth="70"/>
                        <DataGridTextColumn Header="Врач" Binding="{Binding DoctorName}" Width="220" MinWidth="190"/>
                        <DataGridTemplateColumn Header="Анализы" Width="*" MinWidth="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding TestTypes}"
                                           TextWrapping="Wrap"
                                           TextTrimming="CharacterEllipsis"
                                           Padding="8,4"
                                           VerticalAlignment="Center"
                                           ToolTip="{Binding TestTypes}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="130" MinWidth="60"/>
                        <DataGridTemplateColumn Header="Действия" Width="110" MinWidth="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Просмотр" 
                                            HorizontalAlignment="Left"
                                            Tag="{Binding Id}" 
                                            Click="BtnViewDetails_Click"
                                            Background="Transparent"
                                            Foreground="#1976D2"
                                            BorderThickness="0"
                                            Padding="4,1"
                                            Cursor="Hand"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</Page>