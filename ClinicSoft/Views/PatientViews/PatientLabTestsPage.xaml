<Page x:Class="ClinicSoft.Views.PatientViews.PatientLabTestsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Мои анализы">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Мои анализы" 
                   FontSize="24" FontWeight="Bold"
                   Foreground="#1976D2"
                   Margin="0,0,0,16"/>
        <Border Grid.Row="1"
                Background="White"
                BorderBrush="#BDBDBD"
                BorderThickness="1"
                CornerRadius="4"
                Height="36"
                Margin="0,0,0,16">
            <Grid>
                <TextBox x:Name="SearchBox"
                         Background="Transparent"
                         BorderThickness="0"
                         VerticalContentAlignment="Center"
                         Padding="8,0,8,0"
                         TextChanged="SearchBox_TextChanged"
                         Foreground="#212121"
                         VerticalAlignment="Stretch"/>
                <TextBlock x:Name="SearchPlaceholder"
                           Text="Поиск по названию анализа..."
                           Foreground="#9E9E9E"
                           VerticalAlignment="Center"
                           Margin="8,0,8,0"
                           IsHitTestVisible="False"/>
            </Grid>
        </Border>
        <TabControl Grid.Row="2">
            <TabItem Header="Завершённые">
                <DataGrid x:Name="CompletedOrdersGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          RowHeight="60"
                          VerticalScrollBarVisibility="Auto">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Height" Value="36"/>
                            <Setter Property="Padding" Value="8,4"/>
                            <Setter Property="Background" Value="#4CAF50"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Дата" Binding="{Binding CompletedAt, StringFormat='dd.MM.yyyy'}" Width="110" MinWidth="70"/>
                        <DataGridTemplateColumn Header="Анализы" Width="*" MinWidth="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding TestTypes}"
                                               TextWrapping="Wrap"
                                               TextTrimming="CharacterEllipsis"
                                               Padding="8,4"
                                               VerticalAlignment="Center"
                                               ToolTip="{Binding TestTypes}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Действия" Width="110" MinWidth="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Просмотр" 
                                            HorizontalAlignment="Left"
                                            Tag="{Binding LabOrderId}" 
                                            Click="BtnViewDetails_Click"
                                            Background="Transparent"
                                            Foreground="#1976D2"
                                            BorderThickness="0"
                                            Padding="4,1"
                                            Cursor="Hand"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Назначенные">
                <DataGrid x:Name="PendingOrdersGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          RowHeight="60"
                          VerticalScrollBarVisibility="Auto">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Height" Value="36"/>
                            <Setter Property="Padding" Value="8,4"/>
                            <Setter Property="Background" Value="#1976D2"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Дата" Binding="{Binding OrderedAt, StringFormat='dd.MM.yyyy'}" Width="110" MinWidth="70"/>
                        <DataGridTextColumn Header="Врач" Binding="{Binding DoctorName}" Width="*" MinWidth="190"/>
                        <DataGridTemplateColumn Header="Анализы" Width="*" MinWidth="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding TestTypes}"
                                               TextWrapping="Wrap"
                                               TextTrimming="CharacterEllipsis"
                                               Padding="8,4"
                                               VerticalAlignment="Center"
                                               ToolTip="{Binding TestTypes}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="110" MinWidth="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</Page>