<Page x:Class="ClinicSoft.Views.DoctorViews.TreatmentDetailView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:System="clr-namespace:System;assembly=mscorlib"
      Title="Детали плана лечения">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,16">
            <Button x:Name="BtnBack" 
                    Content="Назад" 
                    Padding="12,8"
                    Background="Transparent"
                    BorderThickness="0"
                    Foreground="#1976D2"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Cursor="Hand"
                    Click="BtnBack_Click"
                    Margin="0,0,16,0"/>
            <TextBlock Text="План лечения" 
                       FontSize="20" FontWeight="Bold"
                       Foreground="#1976D2"
                       VerticalAlignment="Center"/>
        </StackPanel>
        <Border Grid.Row="1" 
                Background="#F8F9FA" 
                CornerRadius="8" 
                Padding="16"
                Margin="0,0,0,24">
            <StackPanel>
                <TextBlock><Run FontWeight="Bold">Пациент:</Run> <Run x:Name="PatientNameText"/></TextBlock>
                <TextBlock><Run FontWeight="Bold">Дата назначения:</Run> <Run x:Name="IssuedAtText"/></TextBlock>
                <TextBlock><Run FontWeight="Bold">Статус:</Run> <Run x:Name="StatusText"/></TextBlock>
                <TextBlock><Run FontWeight="Bold">Примечания:</Run> <Run x:Name="NotesText"/></TextBlock>
            </StackPanel>
        </Border>
        <DataGrid x:Name="ItemsGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  RowHeight="60"
                  VerticalScrollBarVisibility="Auto">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Height" Value="36"/>
                    <Setter Property="Padding" Value="8,4"/>
                    <Setter Property="Background" Value="#1976D2"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="*" MinWidth="190"/>
                <DataGridTextColumn Header="Дозировка" Binding="{Binding Dosage}" Width="*" MinWidth="130"/>
                <DataGridTextColumn Header="Длительность" Binding="{Binding Duration}" Width="*" MinWidth="110"/>
                <DataGridTemplateColumn Header="Инструкция" Width="*" MinWidth="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Instructions}"
                                       TextWrapping="Wrap"
                                       TextTrimming="CharacterEllipsis"
                                       Padding="8,4"
                                       ToolTip="{Binding Instructions}"
                                       VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>