<Window x:Class="ClinicSoft.Views.DoctorViews.CreateDocumentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Создание документа" 
        Width="700"
        MinWidth="500"
        MinHeight="500"
        MaxHeight="800"
        WindowStartupLocation="CenterOwner"
        Background="#F8F9FA"
        FontFamily="Segoe UI"
        FontSize="14"
        SizeToContent="Height">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Новый медицинский документ" 
                   Grid.Row="0"
                   FontSize="20" FontWeight="Bold" 
                   Foreground="#1976D2" 
                   Margin="0,0,0,24"/>
        <StackPanel Grid.Row="1" Margin="0,0,0,24">
            <TextBlock Text="Пациент *" Margin="0,0,0,4" Foreground="#212121"/>
            <ComboBox x:Name="CbPatient" 
                      MinHeight="36" 
                      DisplayMemberPath="Display" 
                      SelectedValuePath="Id"
                      Padding="8,0"
                      BorderBrush="#BDBDBD"
                      BorderThickness="1"
                      Background="White"/>
            <TextBlock Text="Тип документа *" Margin="0,12,0,4" Foreground="#212121"/>
            <ComboBox x:Name="CbDocumentType" 
                      MinHeight="36" 
                      DisplayMemberPath="DocumentType.Name" 
                      SelectedValuePath="Id"
                      SelectionChanged="CbDocumentType_SelectionChanged"
                      Padding="8,0"
                      BorderBrush="#BDBDBD"
                      BorderThickness="1"
                      Background="White"/>
        </StackPanel>
        <TextBlock Grid.Row="2" 
                   Text="Содержимое" 
                   Margin="0,0,0,8" 
                   Foreground="#212121"/>
        <Border Grid.Row="3" 
                BorderBrush="#E0E0E0" 
                BorderThickness="1" 
                CornerRadius="4"
                Background="White"
                MinHeight="120"
                Margin="0,0,0,24">
            <TextBox x:Name="TxtContent" 
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled"
                     FontFamily="Consolas"
                     FontSize="13"
                     Padding="12"
                     BorderThickness="0"
                     Background="Transparent"
                     MinHeight="120"
                     VerticalContentAlignment="Top"
                     HorizontalAlignment="Stretch"/>
        </Border>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" 
                    MinWidth="100" Height="36"
                    Padding="16,8"
                    Click="BtnCancel_Click"
                    Background="White"
                    BorderBrush="#1976D2"
                    Foreground="#1976D2"/>
            <Button x:Name="BtnCreate" 
                    Content="Создать документ" 
                    MinWidth="150" Height="36" 
                    Margin="12,0,0,0" 
                    Padding="16,8"
                    Click="BtnCreate_Click"
                    Background="#1976D2" 
                    Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>